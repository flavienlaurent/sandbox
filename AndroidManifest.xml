<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fourmob.sandbox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sbx" >
        <activity android:name=".activities.BaratheonActivity" >
        </activity>
        <activity
            android:name=".activities.LannisterActivity"
            android:theme="@style/Theme.Sbx.Lannister" >
        </activity>
        <activity android:name=".activities.GreyjoyActivity" >
        </activity>
        <activity android:name=".activities.GameOfThronesActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>